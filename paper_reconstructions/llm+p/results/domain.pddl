(define (domain blocksworld-4ops)
    (:requirements :conditional-effects :disjunctive-preconditions :negative-preconditions :strips :typing :universal-preconditions)
    (:types block - object)
    (:predicates (arm-empty) (clear ?x - block)  (holding ?x - block)  (on ?x - block ?y - block)  (on-table ?x - block))
    (:action pickup
        :parameters (?ob - block)
        :precondition (and (clear ?ob) (on-table ?ob) (arm-empty))
        :effect (and (holding ?ob) (not (clear ?ob)) (not (on-table ?ob)) (not (arm-empty)))
    )
     (:action putdown
        :parameters (?ob - block)
        :precondition (holding ?ob)
        :effect (and (clear ?ob) (arm-empty) (on-table ?ob) (not (holding ?ob)))
    )
     (:action stack
        :parameters (?ob - block ?underob - block)
        :precondition (and (clear ?underob) (holding ?ob))
        :effect (and (arm-empty) (clear ?ob) (on ?ob ?underob) (not (clear ?underob)) (not (holding ?ob)))
    )
     (:action unstack
        :parameters (?ob - block ?underob - block)
        :precondition (and (on ?ob ?underob) (clear ?ob) (arm-empty))
        :effect (and (holding ?ob) (clear ?underob) (not (on ?ob ?underob)) (not (clear ?ob)) (not (arm-empty)))
    )
)